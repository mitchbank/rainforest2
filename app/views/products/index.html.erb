<h1>Products</h1>

<%= link_to "Create a product", new_product_path %>

<% @products.each do |product|%>
	<p><%= link_to product.name, "/products/#{product.id}" %>, <%= product.price_in_cents %></p>
<% end %>

